document.write(`<center><h1 style = "color: #016764; font-size: 60px; text-decoration: underline #016764"; > Assignment: 43 to 48 </h1></center>`)
// 1. Show an alert box on click on a link
 function clickHere(){
    alert("Thanks for click")
 }

//  2. Display some Mobile images in browser. On click on an
// image Show the message in alert to user.

function alertBox(){
   alert("Thanks for Purchasing from us!")
}

// 3. Display 10 student records in table and each row should contain a delete
// button. If you click on a button to delete a record, entire row should be
// deleted. 
// before delete





function increase() {
   var num = document.getElementById("counter");
   var count = parseInt(num.innerText);
   num.innerText = count + 1;
}

function decrease() {
   var num = document.getElementById("counter");
   var count = parseInt(num.innerText);
   num.innerText = count - 1;
}